{
  "name": "timzone-list-windows",
  "version": "1.0.0",
  "description": "timezone-list-windows",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "start": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "lite-server": "^2.5.4"
  }
}
